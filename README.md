# CCP Event Calendar

Календарь событий для Cork City Partnership с поддержкой управления событиями, RSVP, комментариями и экспортом.

## 🚀 Быстрый старт

### Локальная разработка

**Требования:**
- Node.js 18+ 
- npm или yarn

**Шаги:**

1. Клонируйте репозиторий:
```bash
git clone https://github.com/ihorvasyliev-gh/CCPFlowCalendar.git
cd CCPFlowCalendar
```

2. Установите зависимости:
```bash
npm install
```

**Примечание:** Если вы планируете использовать Supabase (а не моки), установите также:
```bash
npm install @supabase/supabase-js
```

3. Создайте файл `.env.local` в корне проекта:
```env
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
VITE_CLOUDFLARE_R2_PUBLIC_URL=your_r2_public_url
```

4. Запустите dev сервер:
```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:3000`

## 📦 Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

## 🗄️ Настройка Supabase

### 1. Создание проекта

1. Перейдите на [supabase.com](https://supabase.com)
2. Создайте новый проект
3. Запишите URL проекта и Anon Key из настроек API

### 2. Настройка базы данных

Выполните SQL скрипт из файла **`supabase-setup.sql`** в SQL Editor вашего проекта Supabase.

📖 **Подробные инструкции:** См. файл `SUPABASE_SETUP.md`

Основные таблицы:
- `users` - пользователи системы
- `events` - события календаря
- `event_comments` - комментарии к событиям
- `event_history` - история изменений событий
- `rsvps` - записи на события

### 3. Настройка аутентификации

В Supabase Dashboard:
1. Перейдите в Authentication → Settings
2. Настройте Email авторизацию
3. При необходимости добавьте OAuth провайдеров

### 4. Получение ключей

1. Settings → API
2. Скопируйте:
   - **Project URL** → `VITE_SUPABASE_URL`
   - **anon public** key → `VITE_SUPABASE_ANON_KEY`

## ☁️ Настройка Cloudflare R2 (для постеров)

### 1. Создание R2 Bucket

1. Войдите в [Cloudflare Dashboard](https://dash.cloudflare.com)
2. Перейдите в R2 → Create bucket
3. Создайте bucket (например, `ccp-event-posters`)
4. Запишите название bucket

### 2. Настройка публичного доступа

1. В настройках bucket включите **Public Access**
2. Настройте Custom Domain (опционально) или используйте R2.dev domain
3. Скопируйте публичный URL → `VITE_CLOUDFLARE_R2_PUBLIC_URL`

### 3. Создание API токена

1. R2 → Manage R2 API Tokens
2. Create API Token
3. Выберите нужные права (Read & Write)
4. Сохраните токен (понадобится для backend функций)

## 🌐 Деплой на Cloudflare Pages

### Автоматический деплой через GitHub

1. **Подключите репозиторий:**
   - В Cloudflare Dashboard → Pages → Create a project
   - Подключите GitHub аккаунт
   - Выберите репозиторий `CCPFlowCalendar`

2. **Настройки сборки:**
   - **Build command:** `npm run build`
   - **Build output directory:** `dist`
   - **Root directory:** `/` (или оставьте пустым)

3. **Переменные окружения:**
   Добавьте в настройках проекта:
   - `VITE_SUPABASE_URL` - URL вашего Supabase проекта
   - `VITE_SUPABASE_ANON_KEY` - Anon ключ Supabase
   - `VITE_CLOUDFLARE_R2_PUBLIC_URL` - Публичный URL R2 bucket

4. **Деплой:**
   - Cloudflare автоматически задеплоит при каждом push в main ветку
   - Или нажмите "Retry deployment" для ручного деплоя

### Ручной деплой через Wrangler CLI

1. Установите Wrangler:
```bash
npm install -g wrangler
```

2. Авторизуйтесь:
```bash
wrangler login
```

3. Соберите проект:
```bash
npm run build
```

4. Задеплойте:
```bash
wrangler pages deploy dist --project-name=ccp-event-calendar
```

## 📝 Структура проекта

```
├── components/          # React компоненты
│   ├── CalendarView.tsx
│   ├── EventModal.tsx
│   └── ...
├── contexts/           # React контексты
│   ├── ThemeContext.tsx
│   └── ToastContext.tsx
├── pages/              # Страницы
│   └── LoginPage.tsx
├── services/           # Сервисы для работы с API
│   ├── authService.ts
│   ├── eventService.ts
│   └── ...
├── utils/              # Утилиты
│   ├── date.ts
│   ├── validation.ts
│   └── ...
├── types.ts            # TypeScript типы
├── App.tsx             # Главный компонент
└── index.tsx           # Точка входа
```

## 🔧 Разработка

### Доступные команды

- `npm run dev` - запуск dev сервера
- `npm run build` - сборка для продакшена
- `npm run preview` - предпросмотр production сборки

### Тестовые аккаунты

Для локальной разработки используются моки:
- `admin@ccp.com` - администратор
- `staff@ccp.com` - сотрудник

В продакшене используйте реальную авторизацию через Supabase Auth.

## 📄 Лицензия

Внутреннее использование только для Cork City Partnership.

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте переменные окружения
2. Убедитесь, что Supabase проект настроен корректно
3. Проверьте права доступа к R2 bucket
4. Проверьте консоль браузера на наличие ошибок
